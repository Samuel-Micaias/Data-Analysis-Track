measurement = float(input('Enter meters: '))
cm = measurement * 100
mm = measurement * 1000
km = measurement / 1000
print('The measurement in meters is {}, in centimeters is {}, in millimeters is {}, and in kilometers is {}'.format(measurement, cm, mm, km))
